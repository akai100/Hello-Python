# 1. 创建和使用类

```python3
class Dog:
  def __init__(self, name, age):
    self.name = name
    self.age = age

  def sit(self):
    print(f"{self.name} is now sitting.")
```

# 2. 继承

1. 子类```__init__()```方法

2. 重写父类中的方法
