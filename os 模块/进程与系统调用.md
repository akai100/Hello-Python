## 执行系统命令

```python3
os.system("ls -l")
```

返回值：命令退出码

+ ❌ 不推荐（不安全、不可控）

+ ✅ 推荐 subprocess

## 进程信息

```python3
os.getpid()      # 当前进程 PID
os.getppid()     # 父进程 PID（Unix）
```

## 进程控制（Unix）

```python3
os.fork()
os.execvp("ls", ["ls", "-l"])
```

⚠️ 高级、平台相关
