1. 定义函数

```python3
def greet_user():
    print("Hello!")
```

2. 函数参数

```python3
def greet_user(username):
    print(f"Hello, {username.title()}!"}

greet_user('jesse')
```

3. 关键字实参

```python3
def describe_pet(animal_type, pet_name):
    print(f"\nI have a {animal_type}.")
    print(f"My {animal_type}'s name is {pet_name.title()}.")

describe_pet(animal_type='hamster', pet_name='harry')
```
4. 默认值

```python
def describe_pet(animal_type='dog', pet_name):
    print(f"\nI have a {animal_type}.")
    print(f"My {animal_type}'s name is {pet_name.title()}.")

describe_pet(pet_name='harry')
```

5. 返回值

```python3
def get_formatted_name(first_name, last_name):
    full_name = f"{first_name} {last_name}"
    return full_name.title()
```

6. 传递列表

8. 传递任意数量的实参

```python3
def make_pizza(*toppings):
    print(toppings)

make_pizza('pepperoni')
make_pizza('mushrooms', 'green pepers', 'extra cheese')
```

新参名 ```*toppings``` 中的星号让 python 创建一个名为 toppings 的元组，该元组包含函数收到的所有值，

（1）如果要让函数接受不同类型的实参，必须在函数定义中将接纳任意数量实参的形参放在最后。

```python3
def make_pizza(size, *topping):
    for topping in toppings:
        print(f"- {topping}")
```

（2）使用任意数量的关键字实参

接受任意数量的实参：

```python3
def build_profile(first, last, **user_info):
    user_info['first_name'] = first
    user_info['last_name'] = last
```

**user_info 创建一个名为 user_info 的字典，



